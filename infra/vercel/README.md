# Vercel Deployment Configuration

`vercel.json` in this directory is the canonical Vercel configuration for the monorepo.
Copy it to the repository root (or use it as the source of truth) when deploying the web app.
Keep `infra/vercel/vercel.json` and the root `vercel.json` in sync when making changes.
