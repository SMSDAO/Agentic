# Admin Desktop

This is a placeholder for the admin desktop application.
Do not merge admin-tauri branches into this directory directly.
The `/admin-tauri` directory at the root contains the current Tauri implementation.
